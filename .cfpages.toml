# Cloudflare Pages Configuration
name = "elivate-frontend"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = ".next"

# Build settings
[build]
command = "npm run build"
cwd = ""

# Environment variables for all deployments
[env.production]
NODE_VERSION = "18"
